# frozen_string_literal: true

require "spec_helper"
require "autoscrub"

RSpec.describe AutoScrub do
  xit "does not have any great tests" do
    # But the classes that do the work do.
    expect(1.class.class.to_s).to eq("Class")
  end
end
