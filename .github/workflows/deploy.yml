name: Deploy

on:
  workflow_dispatch:
    inputs:
      branch_hash:
        description: Branch hash (default) or revision to deploy
      environments:
        description: The environment to deploy to
        type: choice
        default: production
        options:
          - production
          - staging
          - testing

jobs:
  deploy:
    permissions:
      contents: read
      packages: write
    name: Deploy to ${{inputs.environments}}
    uses: hathitrust/github_actions/.github/workflows/deploy.yml@dc1e0d1e000e898af7ac65b5ecb1e3f7d9fd1dcc # v2.1.0
    with:
      image: ghcr.io/hathitrust/holdings:${{ inputs.branch_hash || github.sha}}
      file: environments/holdings/${{inputs.environments}}/holdings-client-image.txt
      CONFIG_REPO_RW_APP_ID: ${{ vars.CONFIG_REPO_RW_APP_ID }}
      CONFIG_REPO_FULL_NAME: ${{ vars.CONFIG_REPO_FULL_NAME }}
    secrets: inherit
